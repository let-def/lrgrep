{}

rule error_message = parse
(*| SEMI; LET; ext; list(attribute); rec_flag; val_ident; EQUAL; LIDENT
  { "ok 0 red" }

| strict_binding <- SEMI; 
  LET; ext; list(attribute); rec_flag; val_ident; EQUAL; LIDENT
  { "ok 1 red" }

| structure_item <- SEMI; 
  LET; ext; list(attribute); rec_flag; let_binding_body<-
  { "ok 2 reds" }
*)

| LPAREN; [. RPAREN]; <-
  { "Unclosed parenthesis" }

(*rule main = parse
| ([ . module_expr ] & [ . paren_module_expr ]) <-
  { `Module_expr_struct_paren_module_expr }
| [ . module_type ] <-
  { `Sig_or_lparen_module_type }
| ([ . FOR ] | [ . IF ]) <-
  { `For_If }
|*)
